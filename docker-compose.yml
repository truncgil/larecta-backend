services:
  app:
    build: .
    container_name: larecta_app
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
      - ./docker/supervisor:/etc/supervisor/conf.d
    networks:
      - larecta
    depends_on:
      - db
      - redis
    command: sh -c "supervisord && php-fpm"

  web:
    image: nginx:latest
    container_name: larecta_nginx
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:

      - larecta

  db:
    image: mysql:8
    container_name: larecta_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: larecta
      MYSQL_USER: larecta
      MYSQL_PASSWORD: secret
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - larecta

  redis:
    image: redis:alpine
    container_name: larecta_redis
    restart: always
    networks:
      - larecta

  vite:
    image: node:18-alpine
    container_name: larecta_vite
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
    command: ["sh", "-c", "npm install && npm run dev"]
    ports:
      - "5173:5173"
    networks:
      - larecta

volumes:
  dbdata:

networks:
  larecta:
    driver: bridge
